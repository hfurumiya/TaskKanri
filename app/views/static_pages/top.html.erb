<% provide(:title, "Top") %>

<div class="col-md-6">
  <div class="center">
    <h1 class: "top-title">TaskApp</h1>
    <p>TaskAppは簡易ToDO管理アプリです</p>
    <p>↓↓初めての方はこちら↓↓</p>
    <%= link_to "アカウント作成", signup_path, class: "btn btn-lg btn-success" %>
  </div>
</div>

<div class="col-md-6">
  <div class="center">
    
    <h1>ログイン</h1>
    <% if logged_in? %>
      <%= form_with(scope: :session, url: login_path, local: true) do |f|  %>
        <%= f.label :email, "メールアドレス", class: "label-login" %>
        <%= f.email_field :email, class: 'form-control' %>
        
        <%= f.label :password, "パスワード", class: "label-login"%>
        <%= f.password_field :password, class: 'form-control' %>
        
        <%= f.label :remember_me, class: "checkbox inline checkbox-login", for: "remember-me" do %>
          <%= f.check_box :remember_me, id: "remember-me" %>
          <span>ログイン情報を記憶する</span>
        <% end %>
        
        <%= f.submit "ログイン中です", class: "btn btn-primary btn-block btn-login" %>
      <% end %>
    <% end %>
  </div>
</div>
